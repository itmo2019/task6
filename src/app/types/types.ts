export interface LetterType {
  isVisible: boolean;
  id: string;
  classList: string[];
  text: string[];
  authorAbbr: string;
  author: string;
  subject: string;
  date: string;
  isChecked: boolean;
}
