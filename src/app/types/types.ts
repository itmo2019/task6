export interface LetterType {
  id: string;
  text: string[];
  authorAbbr: string;
  author: string;
  subject: string;
  date: string;
  isVisible: boolean;
  isChecked: boolean;
  hasAddAnimation: boolean;
  hasDeleteAnimation: boolean;
}
