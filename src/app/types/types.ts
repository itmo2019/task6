export interface LetterType {
  id: string;
  text: string[];
  authorAbbr: string;
  author: string;
  subject: string;
  date: string;
  isChecked: boolean;
  hasAddAnimation: boolean;
  hasDeleteAnimation: boolean;
}
